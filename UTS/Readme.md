1.  paradigma pemrograman adalah penulisan program untuk memecahkan masalah dengan menggunakan 
    bahasa pemrograman yang digunakan.

2.  model data  : representasi dari informasi yang disimpan dan digunakan oleh program.contohnya
                  variabel,array,atau struktur data lainnya.dapat didefinisikan sebagai tipe data.

    operator    : simbol atau kata kunci yang digunakan untuk melakukan operasi matematis,logika,atau
                  perbandingan terhadap data.contohnya +,-,*,/,<,>,>=,<=,==,!=

    seleksi     : kondisi atau pilihan yang digunakan untuk mengambil keputusan dalam program.contoh
                  if,else if,dan else.

    fungsi      : digunakan untuk melakukan tugas tertentu dan dapat digunakan berulang kali.contoh
                  sqrt(),pow(),log().

3.  dengan menggunakan perulangan ini,program dapat menghasilkan pola bintang sesuai
    dengan jumlah baris yang dimasukan oleh pengguna.

    variabel'i' : mengulangi sesuai dengan jumlah baris,diulangi sejumlah kali 
                  variabel 'n'.
    variabel's' : untuk mencetak spasi sebelum bintang,diulangi sejumlah kali 
                  variabel'n'.kecuali garis terakhir.
    variabel'j' : mencetak sesuai dengan jumlah bintang pada setiap baris,diulangi 
                  sejumlah kali variabel'i'.                  